spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: "https://github.com/Israel-Ferreira/codekaffe-config-files"
          default-label: main
          search-paths:
            - 'greeting-service*'







server:
  port: 8888


